<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Zavy</title>
    <% include ../../layouts/stylesheet %>
    <% include ../../layouts/script %>
</head>
<body class="body1">
    <br>
    <div class="container1">
        <div class="row">
            <div class="col-md-3"></div>
            <div class="col-md-6 form">
                <form action="/login" id="loginForm" method="POST" class="form-horizontal" role="form">
                    <div class="form-group">
                        <div class="title">
                            <legend class="zavy1"><b>Zavy</b></legend>
                        </div>
                    </div>
        
                    <% if(errorMessage) {%>
                        <div class="alert alert-danger">
                            <p><%= errorMessage %></p>
                        </div>
                    <%}%>
                    
                    <div class="form-group">
                        <label for="email">Địa chỉ mail</label>
                        <input type="text" name="email" class="form-control" id="email" placeholder="Nhập địa chỉ mail" />
                    </div>
        
                    <div class="form-group">
                        <label for="password">Mật khẩu</label>
                        <input type="password" name="password" class="form-control" id="password" placeholder="Nhập mật khẩu" />
                    </div>
                    
                    <div class="form-group">
                        <div class="checkbox">
                            <input type="hidden" name="_csrf" value="{{csrfToken}}" />
                            <button type="submit" onclick = "return validateForm(event)" class="btn btn-primary pull-right">Đăng nhập</button>
                        </div>
                    </div>
                    <a href="#" class="forgot-password">Quên mật khẩu</a>
                </form>
                <br>
                <a href="/register">Bạn chưa có tài khoản? Đăng ký ngay</a>
            </div>
            
        </div>
    </div>
</body>

<script>
    function validateForm(event) {
        var email = document.getElementById("email").value;
        var password = document.getElementById("password").value;

        if(email == ""){
            var mess = '<b>Please enter your e-mail!</b>';
            alertify.error(mess);
            return false;
        }
        else if(password == "") {
            var mess = '<b>Please enter your password!</b>';
            alertify.error(mess);
            return false;
        }
        else {
            $('#loginForm').submit();
            return true;
        }
    }
</script>

</html>
